# Burger Social

A React + Vite social media demo for burger enthusiasts.

## Getting Started

Install dependencies:

```bash
npm run install:all
```

Start the development server:

```bash
npm run dev
```

This runs both the frontend (http://localhost:3000) and backend(mock demo content) API concurrently.

## Build

```bash
npm run build
```
